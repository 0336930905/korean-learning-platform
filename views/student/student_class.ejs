<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= classData.name %> - Chi tiết lớp học</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .glass-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .floating-animation {
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-8px); }
        }
        
        .slide-in {
            animation: slideIn 0.6s ease-out forwards;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .class-header {
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 25px;
            padding: 32px;
            margin-bottom: 32px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(102, 126, 234, 0.3);
        }
        
        .class-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transform: rotate(45deg);
            animation: shimmer 3s ease-in-out infinite;
        }
        
        @keyframes shimmer {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }
        
        .class-icon {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            color: white;
            margin-bottom: 20px;
            animation: float 3s ease-in-out infinite;
        }
        
        .class-title {
            font-size: 32px;
            font-weight: 700;
            color: white;
            margin-bottom: 12px;
        }
        
        .class-subtitle {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
        }
        
        .info-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            position: relative;
            transition: all 0.3s ease;
        }
        
        .info-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #10b981, #34d399);
        }
        
        .info-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 35px 80px rgba(0, 0, 0, 0.15);
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 24px;
            padding: 32px;
        }
        
        .info-item {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 20px;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.05), rgba(118, 75, 162, 0.05));
            border: 2px solid rgba(102, 126, 234, 0.1);
            border-radius: 16px;
            transition: all 0.3s ease;
        }
        
        .info-item:hover {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
            border-color: rgba(102, 126, 234, 0.2);
            transform: translateY(-2px);
        }
        
        .info-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 18px;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        
        .info-content {
            flex: 1;
        }
        
        .info-label {
            font-size: 14px;
            color: #6b7280;
            font-weight: 500;
            margin-bottom: 4px;
        }
        
        .info-value {
            font-size: 16px;
            font-weight: 600;
            color: #374151;
        }
        
        .section-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            position: relative;
            margin-bottom: 32px;
        }
        
        .section-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #3b82f6, #60a5fa);
        }
        
        .section-header {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.05), rgba(96, 165, 250, 0.05));
            border-bottom: 2px solid rgba(59, 130, 246, 0.1);
            padding: 32px;
        }
        
        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: #374151;
            display: flex;
            align-items: center;
            gap: 16px;
        }
        
        .section-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 20px;
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
        }
        
        .category-tabs {
            display: flex;
            gap: 8px;
            margin-bottom: 32px;
            flex-wrap: wrap;
            padding: 0 32px;
        }
        
        .category-tab {
            padding: 12px 24px;
            background: rgba(255, 255, 255, 0.7);
            border: 2px solid rgba(59, 130, 246, 0.1);
            border-radius: 20px;
            color: #6b7280;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 500;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .category-tab:hover {
            background: rgba(59, 130, 246, 0.1);
            border-color: rgba(59, 130, 246, 0.2);
            color: #3b82f6;
        }
        
        .category-tab.active {
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            border-color: transparent;
            color: white;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }
        
        .documents-container {
            padding: 0 32px 32px;
        }
        
        .document-item {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            padding: 24px;
            margin-bottom: 16px;
            transition: all 0.4s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }
        
        .document-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #10b981, #34d399);
        }
        
        .document-item:hover {
            transform: translateY(-4px) scale(1.01);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            border-color: rgba(16, 185, 129, 0.3);
        }
        
        .document-header {
            display: flex;
            align-items: flex-start;
            gap: 20px;
            margin-bottom: 16px;
        }
        
        .file-icon-container {
            width: 60px;
            height: 60px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            position: relative;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        
        .document-item:hover .file-icon-container {
            transform: scale(1.1);
        }
        
        .file-icon {
            font-size: 24px;
        }
        
        .document-info {
            flex: 1;
            min-width: 0;
        }
        
        .document-title {
            font-size: 18px;
            font-weight: 600;
            color: #374151;
            margin-bottom: 8px;
            line-height: 1.3;
        }
        
        .document-description {
            color: #6b7280;
            font-size: 14px;
            line-height: 1.5;
            margin-bottom: 12px;
        }
        
        .document-meta {
            display: flex;
            align-items: center;
            gap: 16px;
            flex-wrap: wrap;
        }
        
        .category-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            border-radius: 16px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border: 1px solid;
        }
        
        .category-speaking {
            background: rgba(59, 130, 246, 0.1);
            color: #3b82f6;
            border-color: rgba(59, 130, 246, 0.2);
        }
        
        .category-listening {
            background: rgba(16, 185, 129, 0.1);
            color: #10b981;
            border-color: rgba(16, 185, 129, 0.2);
        }
        
        .category-writing {
            background: rgba(147, 51, 234, 0.1);
            color: #9333ea;
            border-color: rgba(147, 51, 234, 0.2);
        }
        
        .category-vocabulary {
            background: rgba(245, 158, 11, 0.1);
            color: #f59e0b;
            border-color: rgba(245, 158, 11, 0.2);
        }
        
        .upload-date {
            color: #9ca3af;
            font-size: 12px;
            display: flex;
            align-items: center;
            gap: 4px;
        }
        
        .document-actions {
            display: flex;
            gap: 12px;
            margin-top: 16px;
        }
        
        .action-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 10px 16px;
            border: none;
            border-radius: 12px;
            font-weight: 500;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }
        
        .btn-view {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(96, 165, 250, 0.1));
            color: #3b82f6;
            border: 2px solid rgba(59, 130, 246, 0.2);
        }
        
        .btn-view:hover {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(96, 165, 250, 0.2));
            border-color: rgba(59, 130, 246, 0.3);
            transform: translateY(-2px);
            text-decoration: none;
            color: #3b82f6;
        }
        
        .btn-download {
            background: linear-gradient(135deg, #10b981, #34d399);
            color: white;
            border: 2px solid transparent;
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
        }
        
        .btn-download:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
            text-decoration: none;
            color: white;
        }
        
        .empty-state {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            padding: 60px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .empty-state::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }
        
        .empty-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 32px;
            animation: float 3s ease-in-out infinite;
        }
        
        .empty-icon i {
            font-size: 48px;
            color: #667eea;
        }
        
        .empty-title {
            font-size: 28px;
            font-weight: 700;
            color: #374151;
            margin-bottom: 16px;
        }
        
        .empty-description {
            font-size: 16px;
            color: #6b7280;
            line-height: 1.6;
            max-width: 400px;
            margin: 0 auto;
        }
        
        .back-button {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 16px 24px;
            background: linear-gradient(135deg, rgba(107, 114, 128, 0.1), rgba(156, 163, 175, 0.1));
            border: 2px solid rgba(107, 114, 128, 0.2);
            border-radius: 16px;
            color: #6b7280;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            margin-bottom: 32px;
        }
        
        .back-button:hover {
            background: linear-gradient(135deg, rgba(107, 114, 128, 0.2), rgba(156, 163, 175, 0.2));
            border-color: rgba(107, 114, 128, 0.3);
            color: #374151;
            transform: translateY(-2px);
            text-decoration: none;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 32px;
        }
        
        .stat-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            padding: 24px;
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #10b981, #34d399);
        }
        
        .stat-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.15);
        }
        
        .stat-icon {
            width: 50px;
            height: 50px;
            margin: 0 auto 16px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: white;
        }
        
        .stat-number {
            font-size: 24px;
            font-weight: 700;
            color: #374151;
            margin-bottom: 8px;
        }
        
        .stat-label {
            font-size: 14px;
            color: #6b7280;
            font-weight: 500;
        }
        
        .fade-in {
            animation: fadeIn 0.8s ease-out forwards;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .notification {
            animation: slideInRight 0.3s ease-out;
            z-index: 1000;
        }
        
        @keyframes slideInRight {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
        
        @media (max-width: 768px) {
            .class-header {
                padding: 24px;
                text-align: center;
            }
            
            .class-title {
                font-size: 24px;
            }
            
            .class-subtitle {
                font-size: 16px;
            }
            
            .info-grid {
                grid-template-columns: 1fr;
                gap: 16px;
                padding: 24px;
            }
            
            .info-item {
                padding: 16px;
            }
            
            .section-header {
                padding: 24px;
            }
            
            .section-title {
                font-size: 20px;
                flex-direction: column;
                align-items: flex-start;
                gap: 12px;
            }
            
            .category-tabs {
                padding: 0 24px;
                gap: 8px;
            }
            
            .category-tab {
                padding: 10px 16px;
                font-size: 12px;
            }
            
            .documents-container {
                padding: 0 24px 24px;
            }
            
            .document-header {
                flex-direction: column;
                gap: 16px;
            }
            
            .document-actions {
                flex-direction: column;
                gap: 8px;
            }
        }
        
        /* Document Modal Styles */
        #documentModal {
            animation: fadeIn 0.3s ease-out;
        }
        
        #documentModal .bg-white {
            animation: modalSlideUp 0.3s ease-out;
        }
        
        @keyframes modalSlideUp {
            from {
                opacity: 0;
                transform: translateY(50px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        
        /* Custom scrollbar for modal content */
        #documentContent::-webkit-scrollbar {
            width: 8px;
        }
        
        #documentContent::-webkit-scrollbar-track {
            background: #f1f5f9;
            border-radius: 4px;
        }
        
        #documentContent::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 4px;
        }
        
        #documentContent::-webkit-scrollbar-thumb:hover {
            background: #94a3b8;
        }
        
        /* PDF iframe styling */
        #documentContent iframe {
            border: 1px solid #e2e8f0;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        /* Audio/Video controls styling */
        #documentContent audio,
        #documentContent video {
            outline: none;
            border-radius: 8px;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50 min-h-screen">
    <div class="flex h-screen">
        <!-- Include Student Sidebar -->
        <%- include('../partials/student_sidebar', { 
            user: locals.user, 
            path: '/courses/my-courses' 
        }) %>

        <div class="flex-1 flex flex-col overflow-hidden">
            <%- include('../partials/dashboards_header') %>

            <main class="flex-1 overflow-x-hidden overflow-y-auto">
                <div class="container mx-auto px-6 py-8">
                    <!-- Back Button -->
                    <a href="/courses/<%= classData.course._id %>" class="back-button slide-in">
                        <i class="fas fa-arrow-left"></i>
                        <span>Quay lại khóa học</span>
                    </a>

                    <!-- Class Header -->
                    <div class="class-header slide-in">
                        <div class="class-icon">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                        <div class="class-title"><%= classData.name %></div>
                        <div class="class-subtitle">
                            Khóa học: <%= classData.course.title %>
                        </div>
                    </div>

                    <!-- Class Statistics -->
                    <div class="stats-grid slide-in fade-in" style="animation-delay: 0.1s;">
                        <div class="stat-card">
                            <div class="stat-icon" style="background: linear-gradient(135deg, #3b82f6, #60a5fa);">
                                <i class="fas fa-users"></i>
                            </div>
                            <div class="stat-number"><%= classData.students.length %>/<%= classData.maxStudents %></div>
                            <div class="stat-label">Học viên</div>
                        </div>
                        
                        <div class="stat-card">
                            <div class="stat-icon" style="background: linear-gradient(135deg, #10b981, #34d399);">
                                <i class="fas fa-file-alt"></i>
                            </div>
                            <div class="stat-number"><%= documents ? documents.length : 0 %></div>
                            <div class="stat-label">Tài liệu</div>
                        </div>
                        
                        <div class="stat-card">
                            <div class="stat-icon" style="background: linear-gradient(135deg, #f59e0b, #fbbf24);">
                                <i class="fas fa-calendar-alt"></i>
                            </div>
                            <div class="stat-number">
                                <%= Math.ceil((new Date(classData.endDate) - new Date(classData.startDate)) / (1000 * 60 * 60 * 24 * 7)) %>
                            </div>
                            <div class="stat-label">Tuần học</div>
                        </div>
                        
                        <div class="stat-card">
                            <div class="stat-icon" style="background: linear-gradient(135deg, #8b5cf6, #a78bfa);">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <div class="stat-number"><%= Math.floor(Math.random() * 10) + 5 %></div>
                            <div class="stat-label">Bài tập</div>
                        </div>
                    </div>

                    <!-- Class Information -->
                    <div class="info-card slide-in fade-in" style="animation-delay: 0.2s;">
                        <div class="info-grid">
                            <div class="info-item">
                                <div class="info-icon">
                                    <i class="fas fa-user-tie"></i>
                                </div>
                                <div class="info-content">
                                    <div class="info-label">Giảng viên</div>
                                    <div class="info-value"><%= classData.teacher.fullName %></div>
                                </div>
                            </div>
                            
                            <div class="info-item">
                                <div class="info-icon">
                                    <i class="fas fa-clock"></i>
                                </div>
                                <div class="info-content">
                                    <div class="info-label">Lịch học</div>
                                    <div class="info-value"><%= classData.schedule.days.join(', ') %></div>
                                </div>
                            </div>
                            
                            <div class="info-item">
                                <div class="info-icon">
                                    <i class="fas fa-calendar-alt"></i>
                                </div>
                                <div class="info-content">
                                    <div class="info-label">Ngày bắt đầu</div>
                                    <div class="info-value"><%= new Date(classData.startDate).toLocaleDateString('vi-VN') %></div>
                                </div>
                            </div>
                            
                            <div class="info-item">
                                <div class="info-icon">
                                    <i class="fas fa-calendar-check"></i>
                                </div>
                                <div class="info-content">
                                    <div class="info-label">Ngày kết thúc</div>
                                    <div class="info-value"><%= new Date(classData.endDate).toLocaleDateString('vi-VN') %></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Documents Section -->
                    <div class="section-card slide-in fade-in" style="animation-delay: 0.3s;">
                        <div class="section-header">
                            <div class="section-title">
                                <div class="section-icon">
                                    <i class="fas fa-folder-open"></i>
                                </div>
                                Tài liệu lớp học
                            </div>
                        </div>

                        <!-- Category Tabs -->
                        <div class="category-tabs">
                            <button onclick="filterDocuments('all')" class="category-tab active">
                                <i class="fas fa-th-large"></i>
                                <span>Tất cả</span>
                            </button>
                            <button onclick="filterDocuments('speaking')" class="category-tab">
                                <i class="fas fa-microphone"></i>
                                <span>Luyện nói</span>
                            </button>
                            <button onclick="filterDocuments('listening')" class="category-tab">
                                <i class="fas fa-headphones"></i>
                                <span>Luyện nghe</span>
                            </button>
                            <button onclick="filterDocuments('writing')" class="category-tab">
                                <i class="fas fa-pen"></i>
                                <span>Luyện viết</span>
                            </button>
                            <button onclick="filterDocuments('vocabulary')" class="category-tab">
                                <i class="fas fa-book"></i>
                                <span>Từ vựng</span>
                            </button>
                        </div>
                        
                        <div class="documents-container">
                            <% if (documents && documents.length > 0) { %>
                                <% documents.forEach((doc, index) => { %>
                                    <div class="document-item fade-in" 
                                         data-category="<%= doc.category %>"
                                         style="animation-delay: <%= 0.4 + (index * 0.1) %>s;">
                                        
                                        <div class="document-header">
                                            <!-- File Icon -->
                                            <% 
                                            const fileExt = doc.fileName.split('.').pop().toLowerCase();
                                            let iconClass = '';
                                            let bgClass = '';
                                            
                                            switch(fileExt) {
                                                case 'pdf':
                                                    iconClass = 'fa-file-pdf';
                                                    bgClass = 'background: linear-gradient(135deg, #ef4444, #f87171);';
                                                    break;
                                                case 'doc':
                                                case 'docx':
                                                    iconClass = 'fa-file-word';
                                                    bgClass = 'background: linear-gradient(135deg, #3b82f6, #60a5fa);';
                                                    break;
                                                case 'xls':
                                                case 'xlsx':
                                                    iconClass = 'fa-file-excel';
                                                    bgClass = 'background: linear-gradient(135deg, #10b981, #34d399);';
                                                    break;
                                                case 'ppt':
                                                case 'pptx':
                                                    iconClass = 'fa-file-powerpoint';
                                                    bgClass = 'background: linear-gradient(135deg, #f97316, #fb923c);';
                                                    break;
                                                default:
                                                    iconClass = 'fa-file';
                                                    bgClass = 'background: linear-gradient(135deg, #6b7280, #9ca3af);';
                                            }
                                            %>
                                            
                                            <div class="file-icon-container" style="<%= bgClass %>">
                                                <i class="fas <%= iconClass %> file-icon text-white"></i>
                                            </div>

                                            <!-- Document Info -->
                                            <div class="document-info">
                                                <h3 class="document-title">
                                                    <%= doc.title %>
                                                </h3>
                                                <p class="document-description">
                                                    <%= doc.description || 'Tài liệu học tập hỗ trợ quá trình học tiếng Hàn hiệu quả.' %>
                                                </p>
                                                
                                                <div class="document-meta">
                                                    <!-- Category Badge -->
                                                    <% 
                                                    let categoryClass = '';
                                                    let categoryIcon = '';
                                                    let categoryLabel = '';
                                                    
                                                    switch(doc.category) {
                                                        case 'speaking':
                                                            categoryClass = 'category-speaking';
                                                            categoryIcon = 'fa-microphone';
                                                            categoryLabel = 'Luyện nói';
                                                            break;
                                                        case 'listening':
                                                            categoryClass = 'category-listening';
                                                            categoryIcon = 'fa-headphones';
                                                            categoryLabel = 'Luyện nghe';
                                                            break;
                                                        case 'writing':
                                                            categoryClass = 'category-writing';
                                                            categoryIcon = 'fa-pen';
                                                            categoryLabel = 'Luyện viết';
                                                            break;
                                                        default:
                                                            categoryClass = 'category-vocabulary';
                                                            categoryIcon = 'fa-book';
                                                            categoryLabel = 'Từ vựng';
                                                    }
                                                    %>
                                                    
                                                    <span class="category-badge <%= categoryClass %>">
                                                        <i class="fas <%= categoryIcon %>"></i>
                                                        <%= categoryLabel %>
                                                    </span>
                                                    
                                                    <span class="upload-date">
                                                        <i class="fas fa-clock"></i>
                                                        <%= new Date(doc.uploadedAt).toLocaleDateString('vi-VN') %>
                                                    </span>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Document Actions -->
                                        <div class="document-actions">
                                            <button onclick="openDocumentModal('<%= doc._id %>', '<%= doc.title %>', '<%= doc.fileName %>', '<%= doc.fileType %>')"
                                                   class="action-btn btn-view">
                                                <i class="fas fa-eye"></i>
                                                <span>Xem tài liệu</span>
                                            </button>
                                            
                                            <a href="/student/documents/<%= doc._id %>/download"
                                               class="action-btn btn-download"
                                               onclick="trackDocumentDownload('<%= doc._id %>')">
                                                <i class="fas fa-download"></i>
                                                <span>Tải xuống</span>
                                            </a>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <!-- Empty State -->
                                <div class="empty-state">
                                    <div class="empty-icon">
                                        <i class="fas fa-folder-open"></i>
                                    </div>
                                    <h3 class="empty-title">Chưa có tài liệu</h3>
                                    <p class="empty-description">
                                        Hiện tại chưa có tài liệu nào trong lớp học này. 
                                        Các tài liệu sẽ được giảng viên cập nhật trong quá trình học.
                                    </p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
    
    <!-- Document Preview Modal -->
    <div id="documentModal" class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl max-w-6xl w-full max-h-[90vh] overflow-hidden shadow-2xl">
            <!-- Modal Header -->
            <div class="flex items-center justify-between p-6 border-b border-gray-200">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
                        <i class="fas fa-file-alt text-white"></i>
                    </div>
                    <div>
                        <h3 id="modalDocumentTitle" class="text-xl font-bold text-gray-800">Tài liệu</h3>
                        <p class="text-sm text-gray-500">Xem trước tài liệu</p>
                    </div>
                </div>
                <button onclick="closeDocumentModal()" class="text-gray-400 hover:text-gray-600 transition-colors p-2 hover:bg-gray-100 rounded-lg">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            
            <!-- Modal Content -->
            <div class="p-6 overflow-auto max-h-[calc(90vh-120px)]">
                <div id="documentContent" class="w-full h-full flex items-center justify-center">
                    <div class="flex items-center gap-3 text-gray-500">
                        <i class="fas fa-spinner fa-spin text-2xl"></i>
                        <span>Đang tải tài liệu...</span>
                    </div>
                </div>
            </div>
            
            <!-- Modal Footer -->
            <div class="flex items-center justify-between p-6 border-t border-gray-200 bg-gray-50">
                <div class="text-sm text-gray-500">
                    <i class="fas fa-info-circle mr-2"></i>
                    Sử dụng Ctrl+Wheel để zoom in/out
                </div>
                <div class="flex gap-3">
                    <button onclick="downloadCurrentDocument()" class="flex items-center gap-2 px-4 py-2 bg-green-500 hover:bg-green-600 text-white rounded-lg transition-colors">
                        <i class="fas fa-download"></i>
                        <span>Tải xuống</span>
                    </button>
                    <button onclick="closeDocumentModal()" class="flex items-center gap-2 px-4 py-2 bg-gray-500 hover:bg-gray-600 text-white rounded-lg transition-colors">
                        <i class="fas fa-times"></i>
                        <span>Đóng</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/student_footer') %>

    <script>
        // Filter documents by category
        function filterDocuments(category) {
            // Update active tab
            document.querySelectorAll('.category-tab').forEach(tab => {
                tab.classList.remove('active');
            });
            event.currentTarget.classList.add('active');

            // Filter documents with animation
            const documents = document.querySelectorAll('.document-item');
            documents.forEach((doc, index) => {
                if (category === 'all' || doc.dataset.category === category) {
                    doc.style.display = 'block';
                    doc.style.animationDelay = `${index * 0.1}s`;
                    doc.classList.add('fade-in');
                } else {
                    doc.style.display = 'none';
                }
            });
            
            // Show notification
            const categoryLabels = {
                'all': 'Tất cả tài liệu',
                'speaking': 'Tài liệu luyện nói',
                'listening': 'Tài liệu luyện nghe',
                'writing': 'Tài liệu luyện viết',
                'vocabulary': 'Tài liệu từ vựng'
            };
            
            showNotification(`Hiển thị: ${categoryLabels[category]}`, 'info');
        }

        // Track document interactions
        function trackDocumentView(docId) {
            fetch(`/api/documents/${docId}/track`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ action: 'view' })
            }).catch(console.error);
            
            showNotification('Đang mở tài liệu...', 'info');
        }

        function trackDocumentDownload(docId) {
            fetch(`/api/documents/${docId}/track`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ action: 'download' })
            }).catch(console.error);
            
            showNotification('Đang tải xuống...', 'success');
        }

        // Notification function
        function showNotification(message, type = 'success') {
            const notification = document.createElement('div');
            notification.className = `notification fixed top-6 right-6 z-50 px-6 py-4 rounded-lg shadow-lg text-white font-medium transition-all duration-300`;
            
            switch (type) {
                case 'success':
                    notification.className += ' bg-gradient-to-r from-green-500 to-green-600';
                    break;
                case 'error':
                    notification.className += ' bg-gradient-to-r from-red-500 to-red-600';
                    break;
                case 'info':
                    notification.className += ' bg-gradient-to-r from-blue-500 to-blue-600';
                    break;
                case 'warning':
                    notification.className += ' bg-gradient-to-r from-yellow-500 to-yellow-600';
                    break;
            }
            
            notification.innerHTML = `
                <div class="flex items-center gap-3">
                    <i class="fas ${type === 'success' ? 'fa-check-circle' : 
                                   type === 'error' ? 'fa-exclamation-circle' : 
                                   type === 'warning' ? 'fa-exclamation-triangle' :
                                   'fa-info-circle'}"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.transform = 'translateX(100%)';
                notification.style.opacity = '0';
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }

        // Initialize page
        document.addEventListener('DOMContentLoaded', function() {
            // Add staggered animations
            const elements = document.querySelectorAll('.slide-in, .fade-in');
            elements.forEach((el, index) => {
                if (!el.style.animationDelay) {
                    el.style.animationDelay = `${index * 0.1}s`;
                }
            });

            // Initialize with all documents shown
            filterDocuments('all');
            
            // Add hover effects to document items
            document.querySelectorAll('.document-item').forEach(item => {
                item.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-4px) scale(1.01)';
                });
                
                item.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            // Number keys 1-5 for category filtering
            if (e.key >= '1' && e.key <= '5') {
                const categories = ['all', 'speaking', 'listening', 'writing', 'vocabulary'];
                const categoryIndex = parseInt(e.key) - 1;
                if (categories[categoryIndex]) {
                    const tab = document.querySelectorAll('.category-tab')[categoryIndex];
                    tab.click();
                }
            }
            
            // Escape to go back
            if (e.key === 'Escape') {
                window.history.back();
            }
        });

        // Document modal functions
        let currentDocumentId = null;
        let currentDocumentUrl = null;

        function openDocumentModal(docId, title, fileName, fileType) {
            currentDocumentId = docId;
            currentDocumentUrl = `/uploads/documents/${fileName}`;
            
            // Set modal title
            document.getElementById('modalDocumentTitle').textContent = title;
            
            // Show modal
            document.getElementById('documentModal').classList.remove('hidden');
            
            // Load document content based on file type
            loadDocumentContent(fileName, fileType);
            
            // Track view
            trackDocumentView(docId);
        }

        function closeDocumentModal() {
            document.getElementById('documentModal').classList.add('hidden');
            document.getElementById('documentContent').innerHTML = `
                <div class="flex items-center gap-3 text-gray-500">
                    <i class="fas fa-spinner fa-spin text-2xl"></i>
                    <span>Đang tải tài liệu...</span>
                </div>
            `;
            currentDocumentId = null;
            currentDocumentUrl = null;
        }

        function loadDocumentContent(fileName, fileType) {
            const content = document.getElementById('documentContent');
            const fileUrl = `/uploads/documents/${fileName}`;
            const ext = fileName.split('.').pop().toLowerCase();
            
            // Reset content
            content.innerHTML = '';
            
            switch(ext) {
                case 'pdf':
                    content.innerHTML = `
                        <iframe src="${fileUrl}" 
                                class="w-full h-[600px] border-0 rounded-lg"
                                title="PDF Viewer">
                        </iframe>
                    `;
                    break;
                    
                case 'jpg':
                case 'jpeg':
                case 'png':
                case 'gif':
                    content.innerHTML = `
                        <div class="text-center">
                            <img src="${fileUrl}" 
                                 alt="Document Image" 
                                 class="max-w-full max-h-[600px] object-contain mx-auto rounded-lg shadow-lg">
                        </div>
                    `;
                    break;
                    
                case 'mp3':
                case 'wav':
                case 'm4a':
                    content.innerHTML = `
                        <div class="text-center">
                            <div class="bg-gradient-to-br from-blue-50 to-purple-50 p-8 rounded-2xl">
                                <div class="w-24 h-24 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-6">
                                    <i class="fas fa-music text-white text-3xl"></i>
                                </div>
                                <h3 class="text-xl font-bold text-gray-800 mb-4">File âm thanh</h3>
                                <audio controls class="w-full max-w-md mx-auto">
                                    <source src="${fileUrl}" type="audio/mpeg">
                                    <source src="${fileUrl}" type="audio/wav">
                                    Trình duyệt của bạn không hỗ trợ phát âm thanh.
                                </audio>
                            </div>
                        </div>
                    `;
                    break;
                    
                case 'mp4':
                case 'avi':
                case 'mov':
                    content.innerHTML = `
                        <div class="text-center">
                            <video controls class="w-full max-w-4xl max-h-[600px] mx-auto rounded-lg shadow-lg">
                                <source src="${fileUrl}" type="video/mp4">
                                Trình duyệt của bạn không hỗ trợ phát video.
                            </video>
                        </div>
                    `;
                    break;
                    
                case 'doc':
                case 'docx':
                    content.innerHTML = `
                        <div class="text-center py-12">
                            <div class="bg-gradient-to-br from-blue-50 to-purple-50 p-8 rounded-2xl max-w-md mx-auto">
                                <div class="w-24 h-24 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-6">
                                    <i class="fas fa-file-word text-white text-3xl"></i>
                                </div>
                                <h3 class="text-xl font-bold text-gray-800 mb-4">Tài liệu Word</h3>
                                <p class="text-gray-600 mb-6">Không thể xem trước file Word trong trình duyệt. Vui lòng tải xuống để xem.</p>
                                <button onclick="downloadCurrentDocument()" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg font-medium transition-colors">
                                    <i class="fas fa-download mr-2"></i>
                                    Tải xuống ngay
                                </button>
                            </div>
                        </div>
                    `;
                    break;
                    
                case 'ppt':
                case 'pptx':
                    content.innerHTML = `
                        <div class="text-center py-12">
                            <div class="bg-gradient-to-br from-orange-50 to-red-50 p-8 rounded-2xl max-w-md mx-auto">
                                <div class="w-24 h-24 bg-gradient-to-r from-orange-500 to-red-600 rounded-full flex items-center justify-center mx-auto mb-6">
                                    <i class="fas fa-file-powerpoint text-white text-3xl"></i>
                                </div>
                                <h3 class="text-xl font-bold text-gray-800 mb-4">Bản trình bày PowerPoint</h3>
                                <p class="text-gray-600 mb-6">Không thể xem trước file PowerPoint trong trình duyệt. Vui lòng tải xuống để xem.</p>
                                <button onclick="downloadCurrentDocument()" class="bg-orange-500 hover:bg-orange-600 text-white px-6 py-3 rounded-lg font-medium transition-colors">
                                    <i class="fas fa-download mr-2"></i>
                                    Tải xuống ngay
                                </button>
                            </div>
                        </div>
                    `;
                    break;
                    
                default:
                    content.innerHTML = `
                        <div class="text-center py-12">
                            <div class="bg-gradient-to-br from-gray-50 to-gray-100 p-8 rounded-2xl max-w-md mx-auto">
                                <div class="w-24 h-24 bg-gradient-to-r from-gray-500 to-gray-600 rounded-full flex items-center justify-center mx-auto mb-6">
                                    <i class="fas fa-file text-white text-3xl"></i>
                                </div>
                                <h3 class="text-xl font-bold text-gray-800 mb-4">Tài liệu</h3>
                                <p class="text-gray-600 mb-6">Không thể xem trước loại file này trong trình duyệt. Vui lòng tải xuống để xem.</p>
                                <button onclick="downloadCurrentDocument()" class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-3 rounded-lg font-medium transition-colors">
                                    <i class="fas fa-download mr-2"></i>
                                    Tải xuống ngay
                                </button>
                            </div>
                        </div>
                    `;
                    break;
            }
        }

        function downloadCurrentDocument() {
            if (currentDocumentId && currentDocumentUrl) {
                // Track download
                trackDocumentDownload(currentDocumentId);
                
                // Create temporary link for download
                const link = document.createElement('a');
                link.href = `/student/documents/${currentDocumentId}/download`;
                link.download = '';
                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);
            }
        }

        // Close modal when clicking outside
        document.getElementById('documentModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeDocumentModal();
            }
        });

        // Close modal with Escape key
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && !document.getElementById('documentModal').classList.contains('hidden')) {
                closeDocumentModal();
            }
        });
    </script>
</body>
</html>