<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Xác nhận thanh toán | Korean Learning</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .glass-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, #10b981 0%, #34d399 100%);
        }
        
        .success-gradient {
            background: linear-gradient(135deg, #059669, #10b981);
        }
        
        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .card-hover:hover {
            transform: translateY(-8px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }
        
        .floating-animation {
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-8px); }
        }
        
        .slide-in {
            animation: slideIn 0.6s ease-out forwards;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .success-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #10b981, #34d399);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 60px;
            margin: 0 auto 32px;
            box-shadow: 0 20px 40px rgba(16, 185, 129, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .success-icon::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 100%;
            height: 200%;
            background: rgba(255, 255, 255, 0.1);
            transform: rotate(45deg);
            animation: shimmer 3s infinite;
        }
        
        @keyframes shimmer {
            0%, 100% { transform: translateX(-100%) rotate(45deg); }
            50% { transform: translateX(100%) rotate(45deg); }
        }
        
        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background: #f59e0b;
            border-radius: 50%;
            animation: confetti-fall 3s linear infinite;
        }
        
        @keyframes confetti-fall {
            0% {
                transform: translateY(-100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }
        
        .confetti:nth-child(1) { left: 10%; animation-delay: 0s; background: #ef4444; }
        .confetti:nth-child(2) { left: 20%; animation-delay: 0.5s; background: #3b82f6; }
        .confetti:nth-child(3) { left: 30%; animation-delay: 1s; background: #10b981; }
        .confetti:nth-child(4) { left: 40%; animation-delay: 1.5s; background: #f59e0b; }
        .confetti:nth-child(5) { left: 50%; animation-delay: 2s; background: #8b5cf6; }
        .confetti:nth-child(6) { left: 60%; animation-delay: 0.3s; background: #06b6d4; }
        .confetti:nth-child(7) { left: 70%; animation-delay: 0.8s; background: #84cc16; }
        .confetti:nth-child(8) { left: 80%; animation-delay: 1.3s; background: #ec4899; }
        .confetti:nth-child(9) { left: 90%; animation-delay: 1.8s; background: #f97316; }
        
        .confirmation-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            position: relative;
        }
        
        .course-info {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(52, 211, 153, 0.1));
            border-radius: 20px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid rgba(16, 185, 129, 0.2);
        }
        
        .course-image {
            width: 100px;
            height: 100px;
            border-radius: 16px;
            object-fit: cover;
            border: 3px solid white;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }
        
        .info-row {
            display: flex;
            align-items: center;
            padding: 16px 0;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }
        
        .info-row:last-child {
            border-bottom: none;
        }
        
        .info-row:hover {
            background: rgba(16, 185, 129, 0.05);
            transform: translateX(8px);
            border-radius: 12px;
            padding-left: 16px;
            margin: 0 -16px;
        }
        
        .info-label {
            width: 180px;
            font-weight: 600;
            color: #374151;
            display: flex;
            align-items: center;
        }
        
        .info-value {
            flex: 1;
            color: #6b7280;
            font-weight: 500;
        }
        
        .info-icon {
            margin-right: 8px;
            width: 20px;
            text-align: center;
        }
        
        .btn-3d {
            position: relative;
            transform-style: preserve-3d;
            transition: all 0.3s ease;
            overflow: hidden;
        }
        
        .btn-3d::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transition: all 0.4s ease;
            transform: translate(-50%, -50%);
        }
        
        .btn-3d:hover::before {
            width: 200%;
            height: 200%;
        }
        
        .btn-3d:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            color: white;
            border: none;
            padding: 16px 32px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            text-decoration: none;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }
        
        .btn-primary:hover {
            color: white;
            text-decoration: none;
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
        }
        
        .btn-secondary {
            background: rgba(107, 114, 128, 0.1);
            color: #374151;
            border: 2px solid rgba(107, 114, 128, 0.2);
            padding: 14px 28px;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: all 0.3s ease;
            margin-right: 16px;
        }
        
        .btn-secondary:hover {
            background: rgba(107, 114, 128, 0.2);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(107, 114, 128, 0.2);
            text-decoration: none;
            color: #374151;
        }
        
        .btn-success {
            background: linear-gradient(135deg, #10b981, #34d399);
            color: white;
            border: none;
            padding: 14px 28px;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
            margin-left: 16px;
        }
        
        .btn-success:hover {
            color: white;
            text-decoration: none;
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
        }
        
        .next-steps {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(96, 165, 250, 0.1));
            border-radius: 20px;
            padding: 24px;
            margin-top: 24px;
            border: 1px solid rgba(59, 130, 246, 0.2);
        }
        
        .step-item {
            display: flex;
            align-items: center;
            padding: 12px 0;
            transition: all 0.3s ease;
        }
        
        .step-item:hover {
            transform: translateX(8px);
        }
        
        .step-number {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            margin-right: 16px;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }
        
        .step-text {
            flex: 1;
            font-weight: 500;
            color: #374151;
        }
        
        .celebration-text {
            background: linear-gradient(135deg, #10b981, #34d399);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
        }
        
        .price-highlight {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(52, 211, 153, 0.1));
            color: #059669;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 700;
            border: 1px solid rgba(16, 185, 129, 0.2);
            display: inline-block;
        }
        
        .invoice-badge {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(168, 85, 247, 0.1));
            color: #7c3aed;
            padding: 6px 12px;
            border-radius: 16px;
            font-size: 12px;
            font-weight: 600;
            border: 1px solid rgba(139, 92, 246, 0.2);
            font-family: 'Courier New', monospace;
        }
        
        .payment-method-badge {
            background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(251, 191, 36, 0.1));
            color: #d97706;
            padding: 6px 12px;
            border-radius: 16px;
            font-size: 14px;
            font-weight: 600;
            border: 1px solid rgba(245, 158, 11, 0.2);
        }
        
        .fade-in {
            animation: fadeIn 0.8s ease-out forwards;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .pulse-animation {
            animation: pulse 2s ease-in-out infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .checkmark-animation {
            animation: checkmark 0.6s ease-in-out;
        }
        
        @keyframes checkmark {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        
        @media (max-width: 768px) {
            .confirmation-card {
                margin: 16px;
                border-radius: 20px;
            }
            
            .success-icon {
                width: 100px;
                height: 100px;
                font-size: 50px;
            }
            
            .course-info {
                padding: 16px;
            }
            
            .info-label {
                width: 140px;
                font-size: 14px;
            }
            
            .info-row {
                padding: 12px 0;
            }
            
            .btn-primary,
            .btn-secondary,
            .btn-success {
                width: 100%;
                justify-content: center;
                margin: 8px 0;
            }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-emerald-50 via-teal-50 to-cyan-50 min-h-screen relative overflow-hidden">
    <!-- Confetti Animation -->
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>
    <div class="confetti"></div>

    <div class="flex h-screen relative z-10">
        <!-- Include Student Sidebar -->
        <%- include('../partials/student_sidebar', { 
            user: locals.user, 
            path: '/courses' 
        }) %>

        <div class="flex-1 flex flex-col overflow-hidden">
            <%- include('../partials/dashboards_header') %>

            <main class="flex-1 overflow-x-hidden overflow-y-auto">
                <div class="container mx-auto px-4 py-8 max-w-4xl">
                    <!-- Success Header -->
                    <div class="text-center mb-8 slide-in">
                        <div class="success-icon floating-animation checkmark-animation">
                            <i class="fas fa-check relative z-10"></i>
                        </div>
                        
                        <h1 class="text-5xl font-bold celebration-text mb-4">
                            🎉 Chúc mừng! 🎉
                        </h1>
                        <p class="text-2xl text-gray-600 mb-2">Bạn đã mua khóa học thành công!</p>
                        <p class="text-lg text-gray-500">Hành trình học tập Korean của bạn bắt đầu từ đây</p>
                    </div>

                    <!-- Main Confirmation Card -->
                    <div class="confirmation-card slide-in fade-in card-hover">
                        <!-- Course Information -->
                        <div class="p-8">
                            <div class="course-info">
                                <div class="flex items-center space-x-6">
                                    <img src="<%= course.image || '/images/default-course.jpg' %>" 
                                         alt="<%= course.title %>" 
                                         class="course-image floating-animation">
                                    <div class="flex-1">
                                        <h2 class="text-3xl font-bold text-gray-800 mb-2">
                                            <%= course.title %>
                                        </h2>
                                        <p class="text-gray-600 mb-4">
                                            <%= course.description || 'Khóa học Korean chất lượng cao' %>
                                        </p>
                                        <div class="flex items-center space-x-4">
                                            <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-semibold">
                                                <i class="fas fa-star mr-1"></i>
                                                Đã mua
                                            </span>
                                            <span class="text-gray-500 text-sm">
                                                <i class="fas fa-users mr-1"></i>
                                                Tham gia cùng <%= Math.floor(Math.random() * 1000) + 500 %> học viên khác
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Payment Details -->
                            <div class="space-y-4">
                                <h3 class="text-xl font-bold text-gray-800 mb-6 flex items-center">
                                    <i class="fas fa-receipt mr-3 text-green-500"></i>
                                    Chi tiết thanh toán
                                </h3>

                                <div class="info-row">
                                    <div class="info-label">
                                        <i class="fas fa-tag info-icon text-green-500"></i>
                                        Giá khóa học:
                                    </div>
                                    <div class="info-value">
                                        <span class="price-highlight">
                                            <%= new Intl.NumberFormat('vi-VN').format(invoice.amount) %> VND
                                        </span>
                                    </div>
                                </div>

                                <div class="info-row">
                                    <div class="info-label">
                                        <i class="fas fa-credit-card info-icon text-blue-500"></i>
                                        Phương thức:
                                    </div>
                                    <div class="info-value">
                                        <span class="payment-method-badge">
                                            <i class="fas fa-wallet mr-1"></i>
                                            <%= invoice.paymentMethod %>
                                        </span>
                                    </div>
                                </div>

                                <div class="info-row">
                                    <div class="info-label">
                                        <i class="fas fa-calendar-alt info-icon text-purple-500"></i>
                                        Thời gian:
                                    </div>
                                    <div class="info-value">
                                        <%= new Date(invoice.createdAt).toLocaleString('vi-VN', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </div>
                                </div>

                                <div class="info-row">
                                    <div class="info-label">
                                        <i class="fas fa-hashtag info-icon text-indigo-500"></i>
                                        Mã hóa đơn:
                                    </div>
                                    <div class="info-value">
                                        <span class="invoice-badge">
                                            #<%= invoice._id.toString().toUpperCase().slice(-8) %>
                                        </span>
                                    </div>
                                </div>

                                <div class="info-row">
                                    <div class="info-label">
                                        <i class="fas fa-check-circle info-icon text-green-500"></i>
                                        Trạng thái:
                                    </div>
                                    <div class="info-value">
                                        <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-semibold pulse-animation">
                                            <i class="fas fa-check mr-1"></i>
                                            Thanh toán thành công
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <!-- Next Steps -->
                            <div class="next-steps">
                                <h3 class="text-xl font-bold text-gray-800 mb-4 flex items-center">
                                    <i class="fas fa-route mr-3 text-blue-500"></i>
                                    Bước tiếp theo
                                </h3>
                                
                                <div class="space-y-3">
                                    <div class="step-item">
                                        <div class="step-number">1</div>
                                        <div class="step-text">Truy cập khóa học và xem video giới thiệu</div>
                                    </div>
                                    <div class="step-item" style="animation-delay: 0.1s;">
                                        <div class="step-number">2</div>
                                        <div class="step-text">Tham gia cộng đồng học viên và kết nối với bạn bè</div>
                                    </div>
                                    <div class="step-item" style="animation-delay: 0.2s;">
                                        <div class="step-number">3</div>
                                        <div class="step-text">Bắt đầu học bài đầu tiên và thiết lập mục tiêu</div>
                                    </div>
                                    <div class="step-item" style="animation-delay: 0.3s;">
                                        <div class="step-number">4</div>
                                        <div class="step-text">Tương tác với giảng viên qua Q&A và forum</div>
                                    </div>
                                </div>
                            </div>

                            <!-- Action Buttons -->
                            <div class="flex flex-col md:flex-row items-center justify-center mt-8 space-y-4 md:space-y-0">
                                <a href="/courses" class="btn-secondary btn-3d">
                                    <i class="fas fa-arrow-left mr-2"></i>
                                    Xem thêm khóa học
                                </a>
                                
                                <a href="/courses/my-courses" class="btn-primary btn-3d">
                                    <i class="fas fa-play-circle mr-2"></i>
                                    Bắt đầu học ngay
                                </a>
                                
                                <a href="/student/profile" class="btn-success btn-3d">
                                    <i class="fas fa-user-graduate mr-2"></i>
                                    Xem hồ sơ học tập
                                </a>
                            </div>

                            <!-- Support Information -->
                            <div class="mt-8 p-6 bg-gray-50 rounded-2xl">
                                <div class="text-center">
                                    <h4 class="font-semibold text-gray-800 mb-2">
                                        <i class="fas fa-headset mr-2 text-blue-500"></i>
                                        Cần hỗ trợ?
                                    </h4>
                                    <p class="text-gray-600 mb-4">
                                        Đội ngũ hỗ trợ của chúng tôi luôn sẵn sàng giúp bạn
                                    </p>
                                    <div class="flex flex-col sm:flex-row items-center justify-center space-y-2 sm:space-y-0 sm:space-x-6">
                                        <a href="mailto:support@koreanlearning.vn" 
                                           class="text-blue-600 hover:text-blue-800 transition-colors">
                                            <i class="fas fa-envelope mr-2"></i>
                                            support@koreanlearning.vn
                                        </a>
                                        <a href="tel:+84123456789" 
                                           class="text-blue-600 hover:text-blue-800 transition-colors">
                                            <i class="fas fa-phone mr-2"></i>
                                            (+84) 123 456 789
                                        </a>
                                    </div>
                                </div>
                            </div>

                            <!-- Social Sharing -->
                            <div class="mt-6 text-center">
                                <p class="text-gray-600 mb-4">Chia sẻ thành tựu của bạn:</p>
                                <div class="flex items-center justify-center space-x-4">
                                    <a href="#" onclick="shareToFacebook()" 
                                       class="bg-blue-600 text-white p-3 rounded-full hover:bg-blue-700 transition-colors">
                                        <i class="fab fa-facebook-f"></i>
                                    </a>
                                    <a href="#" onclick="shareToTwitter()" 
                                       class="bg-blue-400 text-white p-3 rounded-full hover:bg-blue-500 transition-colors">
                                        <i class="fab fa-twitter"></i>
                                    </a>
                                    <a href="#" onclick="shareToLinkedIn()" 
                                       class="bg-blue-800 text-white p-3 rounded-full hover:bg-blue-900 transition-colors">
                                        <i class="fab fa-linkedin-in"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script>
        // Animate elements on page load
        document.addEventListener('DOMContentLoaded', function() {
            // Add staggered animations
            const elements = document.querySelectorAll('.slide-in');
            elements.forEach((el, index) => {
                el.style.animationDelay = `${index * 0.1}s`;
            });

            // Auto-hide confetti after 5 seconds
            setTimeout(() => {
                document.querySelectorAll('.confetti').forEach(confetti => {
                    confetti.style.display = 'none';
                });
            }, 5000);

            // Show success notification
            showSuccessNotification();
        });

        // Success notification
        function showSuccessNotification() {
            const notification = document.createElement('div');
            notification.className = 'fixed top-4 right-4 bg-green-500 text-white px-6 py-4 rounded-lg shadow-lg z-50 transform translate-x-full transition-transform duration-300';
            notification.innerHTML = `
                <div class="flex items-center space-x-3">
                    <i class="fas fa-check-circle text-xl"></i>
                    <div>
                        <div class="font-semibold">Thanh toán thành công!</div>
                        <div class="text-sm opacity-90">Bạn có thể bắt đầu học ngay bây giờ</div>
                    </div>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.classList.remove('translate-x-full');
            }, 500);
            
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                setTimeout(() => notification.remove(), 300);
            }, 5000);
        }

        // Social sharing functions
        function shareToFacebook() {
            const url = encodeURIComponent(window.location.href);
            const text = encodeURIComponent('Tôi vừa đăng ký khóa học Korean Learning! 🎉');
            window.open(`https://www.facebook.com/sharer/sharer.php?u=${url}&quote=${text}`, '_blank');
        }

        function shareToTwitter() {
            const url = encodeURIComponent(window.location.href);
            const text = encodeURIComponent('Tôi vừa đăng ký khóa học Korean Learning! 🎉 #KoreanLearning #Education');
            window.open(`https://twitter.com/intent/tweet?url=${url}&text=${text}`, '_blank');
        }

        function shareToLinkedIn() {
            const url = encodeURIComponent(window.location.href);
            window.open(`https://www.linkedin.com/sharing/share-offsite/?url=${url}`, '_blank');
        }

        // Copy invoice ID to clipboard
        document.querySelector('.invoice-badge').addEventListener('click', function() {
            const invoiceId = this.textContent.trim();
            navigator.clipboard.writeText(invoiceId).then(() => {
                // Show copied notification
                const notification = document.createElement('div');
                notification.className = 'fixed bottom-4 right-4 bg-blue-500 text-white px-4 py-2 rounded-lg shadow-lg z-50 text-sm';
                notification.textContent = 'Đã sao chép mã hóa đơn!';
                document.body.appendChild(notification);
                
                setTimeout(() => {
                    notification.remove();
                }, 2000);
            });
        });

        // Add hover effects
        document.querySelectorAll('.info-row').forEach(row => {
            row.addEventListener('mouseenter', function() {
                this.style.backgroundColor = 'rgba(16, 185, 129, 0.05)';
            });
            
            row.addEventListener('mouseleave', function() {
                this.style.backgroundColor = 'transparent';
            });
        });
    </script>
</body>
</html>
