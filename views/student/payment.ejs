<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh toán khóa học | <%= course ? course.title : 'Error' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .glass-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .floating-animation {
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-8px); }
        }
        
        .slide-in {
            animation: slideIn 0.6s ease-out forwards;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .payment-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            position: relative;
            max-width: 900px;
            margin: 0 auto;
        }
        
        .payment-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }
        
        .payment-header {
            background: linear-gradient(135deg, #667eea, #764ba2);
            padding: 32px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .payment-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transform: rotate(45deg);
            animation: shimmer 3s ease-in-out infinite;
        }
        
        @keyframes shimmer {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }
        
        .payment-icon {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 32px;
            color: white;
            animation: float 3s ease-in-out infinite;
        }
        
        .payment-title {
            font-size: 32px;
            font-weight: 700;
            color: white;
            margin-bottom: 12px;
        }
        
        .payment-subtitle {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
        }
        
        .course-info-section {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.05), rgba(118, 75, 162, 0.05));
            border: 2px solid rgba(102, 126, 234, 0.1);
            border-radius: 20px;
            padding: 24px;
            margin: 32px;
            position: relative;
            overflow: hidden;
        }
        
        .course-info-section::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            height: 4px;
            background: linear-gradient(90deg, #10b981, #34d399);
            border-radius: 20px 20px 0 0;
        }
        
        .course-image-container {
            width: 120px;
            height: 120px;
            border-radius: 20px;
            overflow: hidden;
            position: relative;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            flex-shrink: 0;
        }
        
        .course-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.3s ease;
        }
        
        .course-image-container:hover .course-image {
            transform: scale(1.05);
        }
        
        .course-details {
            flex: 1;
            margin-left: 24px;
        }
        
        .course-title {
            font-size: 24px;
            font-weight: 700;
            color: #374151;
            margin-bottom: 12px;
            line-height: 1.3;
        }
        
        .course-description {
            color: #6b7280;
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 16px;
        }
        
        .course-meta {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            margin-bottom: 20px;
        }
        
        .meta-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 12px;
            font-size: 14px;
            color: #374151;
            border: 1px solid rgba(102, 126, 234, 0.1);
        }
        
        .meta-icon {
            font-size: 14px;
            color: #667eea;
        }
        
        .course-price {
            background: linear-gradient(135deg, #10b981, #34d399);
            color: white;
            font-size: 28px;
            font-weight: 700;
            padding: 16px 24px;
            border-radius: 16px;
            text-align: center;
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.3);
            margin-top: 16px;
        }
        
        .payment-methods-section {
            padding: 32px;
        }
        
        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: #374151;
            margin-bottom: 24px;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .section-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }
        
        .payment-methods-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 32px;
        }
        
        .payment-option {
            background: rgba(255, 255, 255, 0.9);
            border: 2px solid rgba(102, 126, 234, 0.1);
            border-radius: 20px;
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .payment-option::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(102, 126, 234, 0.1), transparent);
            transition: left 0.5s ease;
        }
        
        .payment-option:hover::before {
            left: 100%;
        }
        
        .payment-option:hover {
            transform: translateY(-4px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.15);
            border-color: rgba(102, 126, 234, 0.3);
        }
        
        .payment-option.selected {
            border-color: #667eea;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
            transform: translateY(-4px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.2);
        }
        
        .payment-method-header {
            display: flex;
            align-items: center;
            justify-content: between;
            margin-bottom: 16px;
        }
        
        .payment-method-info {
            display: flex;
            align-items: center;
            gap: 16px;
            flex: 1;
        }
        
        .payment-method-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: #667eea;
            position: relative;
        }
        
        .payment-method-icon img {
            width: 40px;
            height: 40px;
            object-fit: contain;
        }
        
        .method-name {
            font-size: 18px;
            font-weight: 600;
            color: #374151;
            margin-bottom: 4px;
        }
        
        .method-description {
            font-size: 14px;
            color: #6b7280;
            line-height: 1.4;
        }
        
        .selection-indicator {
            width: 24px;
            height: 24px;
            border: 2px solid #d1d5db;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .payment-option.selected .selection-indicator {
            border-color: #667eea;
            background: #667eea;
        }
        
        .selection-indicator::after {
            content: '';
            width: 8px;
            height: 8px;
            background: white;
            border-radius: 50%;
            transform: scale(0);
            transition: transform 0.2s ease;
        }
        
        .payment-option.selected .selection-indicator::after {
            transform: scale(1);
        }
        
        .method-features {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 12px;
        }
        
        .feature-tag {
            background: rgba(16, 185, 129, 0.1);
            color: #10b981;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
            border: 1px solid rgba(16, 185, 129, 0.2);
        }
        
        .security-badge {
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
            padding: 12px 16px;
            border-radius: 12px;
            margin-top: 24px;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }
        
        .security-icon {
            font-size: 16px;
        }
        
        .action-section {
            background: linear-gradient(135deg, rgba(248, 250, 252, 0.8), rgba(241, 245, 249, 0.8));
            border-top: 2px solid rgba(102, 126, 234, 0.1);
            padding: 32px;
            display: flex;
            align-items: center;
            justify-content: between;
            gap: 24px;
        }
        
        .back-button {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px 24px;
            background: rgba(107, 114, 128, 0.1);
            border: 2px solid rgba(107, 114, 128, 0.2);
            border-radius: 16px;
            color: #6b7280;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .back-button:hover {
            background: rgba(107, 114, 128, 0.2);
            border-color: rgba(107, 114, 128, 0.3);
            color: #374151;
            transform: translateY(-2px);
            text-decoration: none;
        }
        
        .proceed-button {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px 32px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border: none;
            border-radius: 16px;
            color: white;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .proceed-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }
        
        .proceed-button:hover::before {
            left: 100%;
        }
        
        .proceed-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
        }
        
        .proceed-button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.2);
        }
        
        .loading-spinner {
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        .error-state {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            padding: 60px 40px;
            text-align: center;
            max-width: 500px;
            margin: 0 auto;
        }
        
        .error-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(248, 113, 113, 0.1));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 24px;
            font-size: 32px;
            color: #ef4444;
            animation: float 3s ease-in-out infinite;
        }
        
        .error-title {
            font-size: 28px;
            font-weight: 700;
            color: #374151;
            margin-bottom: 16px;
        }
        
        .error-description {
            font-size: 16px;
            color: #6b7280;
            line-height: 1.6;
            margin-bottom: 32px;
        }
        
        .error-button {
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            color: white;
            padding: 16px 32px;
            border: none;
            border-radius: 16px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
        }
        
        .error-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(59, 130, 246, 0.4);
            text-decoration: none;
            color: white;
        }
        
        .trust-indicators {
            display: flex;
            justify-content: center;
            gap: 24px;
            margin-top: 32px;
            padding-top: 24px;
            border-top: 1px solid rgba(102, 126, 234, 0.1);
        }
        
        .trust-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #6b7280;
            font-size: 14px;
        }
        
        .trust-icon {
            color: #10b981;
        }
        
        @media (max-width: 768px) {
            .payment-container {
                margin: 16px;
                border-radius: 20px;
            }
            
            .payment-header {
                padding: 24px;
            }
            
            .payment-title {
                font-size: 24px;
            }
            
            .payment-subtitle {
                font-size: 16px;
            }
            
            .course-info-section {
                margin: 24px 16px;
                padding: 20px;
            }
            
            .course-details {
                margin-left: 0;
                margin-top: 16px;
            }
            
            .course-title {
                font-size: 20px;
            }
            
            .course-meta {
                flex-direction: column;
                gap: 12px;
            }
            
            .payment-methods-section {
                padding: 24px 16px;
            }
            
            .payment-methods-grid {
                grid-template-columns: 1fr;
                gap: 16px;
            }
            
            .action-section {
                padding: 24px 16px;
                flex-direction: column;
                gap: 16px;
            }
            
            .back-button,
            .proceed-button {
                width: 100%;
                justify-content: center;
            }
            
            .trust-indicators {
                flex-direction: column;
                gap: 12px;
                text-align: center;
            }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50 min-h-screen">
    <% if (course) { %>
        <div class="min-h-screen flex items-center justify-center p-4">
            <div class="payment-container slide-in">
                <!-- Payment Header -->
                <div class="payment-header">
                    <div class="payment-icon">
                        <i class="fas fa-credit-card"></i>
                    </div>
                    <h1 class="payment-title">Thanh toán khóa học</h1>
                    <p class="payment-subtitle">Hoàn tất thanh toán để bắt đầu học tập</p>
                </div>

                <!-- Course Information -->
                <div class="course-info-section">
                    <div class="flex flex-col md:flex-row items-start">
                        <div class="course-image-container">
                            <img src="<%= course.imageUrl || '/images/default-course.jpg' %>" 
                                 alt="<%= course.title %>" 
                                 class="course-image"
                                 onerror="this.onerror=null; this.src='/images/default-course.jpg';">
                        </div>
                        
                        <div class="course-details">
                            <h2 class="course-title"><%= course.title %></h2>
                            <p class="course-description">
                                <%= course.description || 'Khóa học tiếng Hàn chất lượng cao với phương pháp giảng dạy hiện đại và tương tác.' %>
                            </p>
                            
                            <div class="course-meta">
                                <div class="meta-item">
                                    <i class="fas fa-clock meta-icon"></i>
                                    <span><%= course.duration || '24' %> giờ học</span>
                                </div>
                                <div class="meta-item">
                                    <i class="fas fa-list-ul meta-icon"></i>
                                    <span><%= Math.floor(Math.random() * 20) + 10 %> bài học</span>
                                </div>
                                <div class="meta-item">
                                    <i class="fas fa-certificate meta-icon"></i>
                                    <span>Có chứng chỉ</span>
                                </div>
                                <div class="meta-item">
                                    <i class="fas fa-users meta-icon"></i>
                                    <span><%= Math.floor(Math.random() * 500) + 50 %> học viên</span>
                                </div>
                            </div>
                            
                            <div class="course-price">
                                <%= new Intl.NumberFormat('vi-VN', { 
                                    style: 'currency', 
                                    currency: 'VND' 
                                }).format(course.price) %>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Payment Methods Section -->
                <div class="payment-methods-section">
                    <h3 class="section-title">
                        <div class="section-icon">
                            <i class="fas fa-wallet"></i>
                        </div>
                        Chọn phương thức thanh toán
                    </h3>

                    <div class="payment-methods-grid">
                        <!-- ZaloPay App -->
                        <div class="payment-option" data-method="zalopay_app">
                            <div class="payment-method-header">
                                <div class="payment-method-info">
                                    <div class="payment-method-icon">
                                        <img src="https://th.bing.com/th/id/OIP.dqBGQAVmIM35FLHSndWGLwHaHa?w=168&h=180&c=7&r=0&o=5&dpr=1.3&pid=1.7" alt="ZaloPay">
                                    </div>
                                    <div>
                                        <div class="method-name">ZaloPay App</div>
                                        <div class="method-description">Thanh toán nhanh chóng qua ứng dụng ZaloPay</div>
                                    </div>
                                </div>
                                <div class="selection-indicator"></div>
                            </div>
                            <div class="method-features">
                                <span class="feature-tag">Nhanh chóng</span>
                                <span class="feature-tag">Bảo mật</span>
                                <span class="feature-tag">Ưu đãi</span>
                            </div>
                        </div>

                        <!-- ZaloPay QR -->
                        <div class="payment-option" data-method="zalopay_qr">
                            <div class="payment-method-header">
                                <div class="payment-method-info">
                                    <div class="payment-method-icon">
                                        <i class="fas fa-qrcode"></i>
                                    </div>
                                    <div>
                                        <div class="method-name">ZaloPay QR</div>
                                        <div class="method-description">Quét mã QR để thanh toán trực tiếp</div>
                                    </div>
                                </div>
                                <div class="selection-indicator"></div>
                            </div>
                            <div class="method-features">
                                <span class="feature-tag">Không cần app</span>
                                <span class="feature-tag">Tiện lợi</span>
                            </div>
                        </div>

                        <!-- ZaloPay Credit Card -->
                        <div class="payment-option" data-method="zalopay_cc">
                            <div class="payment-method-header">
                                <div class="payment-method-info">
                                    <div class="payment-method-icon">
                                        <i class="fas fa-credit-card"></i>
                                    </div>
                                    <div>
                                        <div class="method-name">Thẻ tín dụng/ghi nợ</div>
                                        <div class="method-description">Visa, Mastercard, JCB và các loại thẻ khác</div>
                                    </div>
                                </div>
                                <div class="selection-indicator"></div>
                            </div>
                            <div class="method-features">
                                <span class="feature-tag">Quốc tế</span>
                                <span class="feature-tag">An toàn</span>
                                <span class="feature-tag">Trả góp</span>
                            </div>
                        </div>

                        <!-- ZaloPay ATM -->
                        <div class="payment-option" data-method="zalopay_atm">
                            <div class="payment-method-header">
                                <div class="payment-method-info">
                                    <div class="payment-method-icon">
                                        <i class="fas fa-university"></i>
                                    </div>
                                    <div>
                                        <div class="method-name">Thẻ ATM nội địa</div>
                                        <div class="method-description">Hỗ trợ tất cả ngân hàng tại Việt Nam</div>
                                    </div>
                                </div>
                                <div class="selection-indicator"></div>
                            </div>
                            <div class="method-features">
                                <span class="feature-tag">Phổ biến</span>
                                <span class="feature-tag">Đáng tin cậy</span>
                            </div>
                        </div>
                    </div>

                    <!-- Security Badge -->
                    <div class="security-badge">
                        <i class="fas fa-shield-alt security-icon"></i>
                        <div>
                            <strong>Bảo mật SSL 256-bit</strong> - Thông tin thanh toán của bạn được mã hóa và bảo vệ tuyệt đối
                        </div>
                    </div>

                    <!-- Trust Indicators -->
                    <div class="trust-indicators">
                        <div class="trust-item">
                            <i class="fas fa-lock trust-icon"></i>
                            <span>Thanh toán bảo mật</span>
                        </div>
                        <div class="trust-item">
                            <i class="fas fa-undo-alt trust-icon"></i>
                            <span>Hoàn tiền 100%</span>
                        </div>
                        <div class="trust-item">
                            <i class="fas fa-headset trust-icon"></i>
                            <span>Hỗ trợ 24/7</span>
                        </div>
                    </div>
                </div>

                <!-- Action Section -->
                <div class="action-section">
                    <a href="/courses/<%= course._id %>" class="back-button">
                        <i class="fas fa-arrow-left"></i>
                        <span>Quay lại khóa học</span>
                    </a>
                    
                    <button id="proceedPayment" class="proceed-button" disabled>
                        <span>Tiếp tục thanh toán</span>
                        <i class="fas fa-arrow-right"></i>
                    </button>
                </div>
            </div>
        </div>
    <% } else { %>
        <!-- Error State -->
        <div class="min-h-screen flex items-center justify-center p-4">
            <div class="error-state slide-in">
                <div class="error-icon">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <h1 class="error-title">Có lỗi xảy ra</h1>
                <p class="error-description">
                    Không thể tải thông tin khóa học. Vui lòng thử lại sau hoặc liên hệ với chúng tôi để được hỗ trợ.
                </p>
                <a href="/courses" class="error-button">
                    <i class="fas fa-arrow-left"></i>
                    <span>Quay lại danh sách khóa học</span>
                </a>
            </div>
        </div>
    <% } %>

    <script>
        const paymentOptions = document.querySelectorAll('.payment-option');
        const proceedButton = document.getElementById('proceedPayment');
        let selectedMethod = null;

        // Handle payment method selection
        paymentOptions.forEach(option => {
            option.addEventListener('click', function() {
                // Remove selected state from all options
                paymentOptions.forEach(opt => {
                    opt.classList.remove('selected');
                });

                // Add selected state to clicked option
                this.classList.add('selected');
                
                selectedMethod = this.dataset.method;
                proceedButton.disabled = false;
                
                // Show selection feedback
                showNotification('Đã chọn phương thức thanh toán', 'success');
            });
        });

        // Handle payment processing
        proceedButton.addEventListener('click', async () => {
            if (!selectedMethod) {
                showNotification('Vui lòng chọn phương thức thanh toán', 'error');
                return;
            }

            try {
                // Show loading state
                proceedButton.disabled = true;
                proceedButton.innerHTML = `
                    <i class="fas fa-spinner loading-spinner"></i>
                    <span>Đang xử lý thanh toán...</span>
                `;

                const response = await fetch('/payment/process', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        courseId: '<%= course._id %>',
                        paymentMethod: selectedMethod,
                        amount: <%= course.price %>
                    })
                });

                const result = await response.json();
                
                if (result.success) {
                    // Store payment ID in session storage
                    sessionStorage.setItem('currentPaymentId', result.paymentId);
                    
                    showNotification('Đang chuyển hướng đến cổng thanh toán...', 'info');
                    
                    setTimeout(() => {
                        window.location.href = result.paymentUrl;
                    }, 1000);
                } else {
                    throw new Error(result.message || 'Có lỗi xảy ra khi xử lý thanh toán');
                }
            } catch (error) {
                console.error('Payment error:', error);
                showNotification(error.message || 'Có lỗi xảy ra khi xử lý thanh toán', 'error');
                
                // Reset button state
                proceedButton.disabled = selectedMethod ? false : true;
                proceedButton.innerHTML = `
                    <span>Tiếp tục thanh toán</span>
                    <i class="fas fa-arrow-right"></i>
                `;
            }
        });

        // Notification function
        function showNotification(message, type = 'success') {
            const notification = document.createElement('div');
            notification.className = `fixed top-6 right-6 z-50 px-6 py-4 rounded-lg shadow-lg text-white font-medium transition-all duration-300 transform translate-x-full`;
            
            switch (type) {
                case 'success':
                    notification.className += ' bg-gradient-to-r from-green-500 to-green-600';
                    break;
                case 'error':
                    notification.className += ' bg-gradient-to-r from-red-500 to-red-600';
                    break;
                case 'info':
                    notification.className += ' bg-gradient-to-r from-blue-500 to-blue-600';
                    break;
            }
            
            notification.innerHTML = `
                <div class="flex items-center gap-3">
                    <i class="fas ${type === 'success' ? 'fa-check-circle' : 
                                   type === 'error' ? 'fa-exclamation-circle' : 
                                   'fa-info-circle'}"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.classList.remove('translate-x-full');
            }, 100);
            
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }

        // Handle payment cancellation
        window.addEventListener('beforeunload', () => {
            const paymentId = sessionStorage.getItem('currentPaymentId');
            if (paymentId) {
                fetch(`/payment/cancel/${paymentId}`, { method: 'POST' })
                    .then(() => sessionStorage.removeItem('currentPaymentId'))
                    .catch(console.error);
            }
        });

        // Initialize animations
        document.addEventListener('DOMContentLoaded', function() {
            // Add staggered animations for payment options
            paymentOptions.forEach((option, index) => {
                option.style.animationDelay = `${index * 0.1}s`;
                option.classList.add('slide-in');
            });
        });
    </script>
</body>
</html>