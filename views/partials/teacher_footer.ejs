<footer class="teacher-footer">
    <!-- Wave Animation -->
    <div class="wave-container">
        <svg class="wave" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
            <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z" class="wave-fill"></path>
        </svg>
    </div>

    <div class="footer-content">
        <!-- Main Footer Content -->
        <div class="max-w-7xl mx-auto px-6 py-12">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
                <!-- Teaching Resources -->
                <div class="footer-section teaching-section slide-up" data-delay="0.1">
                    <div class="section-header">
                        <div class="section-icon">
                            <i class="fas fa-chalkboard-teacher"></i>
                        </div>
                        <h3 class="section-title">Tài nguyên giảng dạy</h3>
                    </div>
                    
                    <ul class="resource-list">
                        <li class="resource-item">
                            <a href="/teacher/course-templates" class="resource-link">
                                <div class="resource-icon">
                                    <i class="fas fa-file-alt"></i>
                                </div>
                                <div class="resource-info">
                                    <span class="resource-title">Mẫu khóa học</span>
                                    <span class="resource-desc">Templates cho các bài giảng</span>
                                </div>
                            </a>
                        </li>
                        <li class="resource-item">
                            <a href="/teacher/assessment-tools" class="resource-link">
                                <div class="resource-icon">
                                    <i class="fas fa-clipboard-check"></i>
                                </div>
                                <div class="resource-info">
                                    <span class="resource-title">Công cụ đánh giá</span>
                                    <span class="resource-desc">Tạo bài kiểm tra và quiz</span>
                                </div>
                            </a>
                        </li>
                        <li class="resource-item">
                            <a href="/teacher/media-library" class="resource-link">
                                <div class="resource-icon">
                                    <i class="fas fa-photo-video"></i>
                                </div>
                                <div class="resource-info">
                                    <span class="resource-title">Thư viện media</span>
                                    <span class="resource-desc">Hình ảnh và video giảng dạy</span>
                                </div>
                            </a>
                        </li>
                        <li class="resource-item">
                            <a href="/teacher/lesson-planner" class="resource-link">
                                <div class="resource-icon">
                                    <i class="fas fa-calendar-alt"></i>
                                </div>
                                <div class="resource-info">
                                    <span class="resource-title">Lập kế hoạch bài học</span>
                                    <span class="resource-desc">Công cụ lên lịch giảng dạy</span>
                                </div>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Class Management -->
                <div class="footer-section class-section slide-up" data-delay="0.2">
                    <div class="section-header">
                        <div class="section-icon">
                            <i class="fas fa-users-cog"></i>
                        </div>
                        <h3 class="section-title">Quản lý lớp học</h3>
                    </div>
                    
                    <ul class="management-links">
                        <li class="management-item">
                            <a href="/teacher/student-management" class="management-link">
                                <div class="link-icon">
                                    <i class="fas fa-user-graduate"></i>
                                </div>
                                <span class="link-text">Quản lý học sinh</span>
                                <div class="link-arrow">
                                    <i class="fas fa-chevron-right"></i>
                                </div>
                            </a>
                        </li>
                        <li class="management-item">
                            <a href="/teacher/gradebook" class="management-link">
                                <div class="link-icon">
                                    <i class="fas fa-book"></i>
                                </div>
                                <span class="link-text">Sổ điểm điện tử</span>
                                <div class="link-arrow">
                                    <i class="fas fa-chevron-right"></i>
                                </div>
                            </a>
                        </li>
                        <li class="management-item">
                            <a href="/teacher/attendance" class="management-link">
                                <div class="link-icon">
                                    <i class="fas fa-calendar-check"></i>
                                </div>
                                <span class="link-text">Điểm danh</span>
                                <div class="link-arrow">
                                    <i class="fas fa-chevron-right"></i>
                                </div>
                            </a>
                        </li>
                        <li class="management-item">
                            <a href="/teacher/reports" class="management-link">
                                <div class="link-icon">
                                    <i class="fas fa-chart-bar"></i>
                                </div>
                                <span class="link-text">Báo cáo tiến độ</span>
                                <div class="link-arrow">
                                    <i class="fas fa-chevron-right"></i>
                                </div>
                            </a>
                        </li>
                        <li class="management-item">
                            <a href="/teacher/parent-communication" class="management-link">
                                <div class="link-icon">
                                    <i class="fas fa-comments"></i>
                                </div>
                                <span class="link-text">Liên hệ phụ huynh</span>
                                <div class="link-arrow">
                                    <i class="fas fa-chevron-right"></i>
                                </div>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Professional Development -->
                <div class="footer-section development-section slide-up" data-delay="0.3">
                    <div class="section-header">
                        <div class="section-icon">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                        <h3 class="section-title">Phát triển chuyên môn</h3>
                    </div>
                    
                    <ul class="development-links">
                        <li class="development-item">
                            <a href="/teacher/training" class="development-link">
                                <div class="dev-icon">
                                    <i class="fas fa-certificate"></i>
                                </div>
                                <div class="dev-info">
                                    <span class="dev-title">Khóa đào tạo</span>
                                    <span class="dev-desc">Nâng cao kỹ năng giảng dạy</span>
                                </div>
                            </a>
                        </li>
                        <li class="development-item">
                            <a href="/teacher/webinars" class="development-link">
                                <div class="dev-icon">
                                    <i class="fas fa-video"></i>
                                </div>
                                <div class="dev-info">
                                    <span class="dev-title">Webinar & Hội thảo</span>
                                    <span class="dev-desc">Cập nhật xu hướng giáo dục</span>
                                </div>
                            </a>
                        </li>
                        <li class="development-item">
                            <a href="/teacher/community" class="development-link">
                                <div class="dev-icon">
                                    <i class="fas fa-users"></i>
                                </div>
                                <div class="dev-info">
                                    <span class="dev-title">Cộng đồng giáo viên</span>
                                    <span class="dev-desc">Chia sẻ kinh nghiệm</span>
                                </div>
                            </a>
                        </li>
                        <li class="development-item">
                            <a href="/teacher/research" class="development-link">
                                <div class="dev-icon">
                                    <i class="fas fa-search"></i>
                                </div>
                                <div class="dev-info">
                                    <span class="dev-title">Nghiên cứu giáo dục</span>
                                    <span class="dev-desc">Tài liệu và bài viết</span>
                                </div>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Support & Contact -->
                <div class="footer-section support-section slide-up" data-delay="0.4">
                    <div class="section-header">
                        <div class="section-icon">
                            <i class="fas fa-headset"></i>
                        </div>
                        <h3 class="section-title">Hỗ trợ giáo viên</h3>
                    </div>
                    
                    <!-- Contact Info -->
                    <div class="contact-info">
                        <div class="contact-item">
                            <div class="contact-icon">
                                <i class="fas fa-phone"></i>
                            </div>
                            <div class="contact-details">
                                <span class="contact-text">Hotline: (028) 1234-5678</span>
                                <span class="contact-sub">Hỗ trợ 24/7</span>
                            </div>
                        </div>
                        <div class="contact-item">
                            <div class="contact-icon">
                                <i class="fas fa-envelope"></i>
                            </div>
                            <div class="contact-details">
                                <span class="contact-text">teacher@korea-db.com</span>
                                <span class="contact-sub">Email hỗ trợ giáo viên</span>
                            </div>
                        </div>
                    </div>

                    <!-- Quick Support -->
                    <div class="quick-support">
                        <h4 class="support-title">
                            <i class="fas fa-bolt mr-2"></i>
                            Hỗ trợ nhanh
                        </h4>
                        <div class="support-buttons">
                            <button class="support-btn primary" onclick="openLiveChat()">
                                <i class="fas fa-comments"></i>
                                <span>Chat trực tiếp</span>
                            </button>
                            <button class="support-btn secondary" onclick="openTicket()">
                                <i class="fas fa-ticket-alt"></i>
                                <span>Tạo ticket</span>
                            </button>
                        </div>
                    </div>

                    <!-- Social Links for Teachers -->
                    <div class="teacher-social">
                        <h4 class="social-title">Kết nối với chúng tôi</h4>
                        <div class="social-links">
                            <a href="#" class="social-link facebook" title="Facebook giáo viên">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a href="#" class="social-link linkedin" title="LinkedIn giáo viên">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a href="#" class="social-link youtube" title="YouTube kênh giáo viên">
                                <i class="fab fa-youtube"></i>
                            </a>
                            <a href="#" class="social-link discord" title="Discord cộng đồng">
                                <i class="fab fa-discord"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Teacher Stats Section -->
        <div class="teacher-stats-section">
            <div class="max-w-7xl mx-auto px-6 py-8">
                <div class="stats-header">
                    <h3 class="stats-title">Thống kê hệ thống giảng dạy</h3>
                    <p class="stats-subtitle">Cộng đồng giáo viên và học sinh đang phát triển</p>
                </div>
                <div class="stats-grid">
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-chalkboard-teacher"></i>
                        </div>
                        <div class="stat-number" data-target="1250">0</div>
                        <div class="stat-label">Giáo viên</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <div class="stat-number" data-target="15000">0</div>
                        <div class="stat-label">Học sinh</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-book"></i>
                        </div>
                        <div class="stat-number" data-target="800">0</div>
                        <div class="stat-label">Khóa học</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-trophy"></i>
                        </div>
                        <div class="stat-number" data-target="25">0</div>
                        <div class="stat-label">Giải thưởng</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer Bottom -->
        <div class="footer-bottom">
            <div class="max-w-7xl mx-auto px-6 py-6">
                <div class="bottom-content">
                    <div class="copyright-section">
                        <div class="footer-logo">
                            <div class="logo-icon">
                                <i class="fas fa-graduation-cap"></i>
                            </div>
                            <div class="logo-text">
                                <span class="logo-title">Korea-DB</span>
                                <span class="logo-subtitle">Teacher Portal</span>
                            </div>
                        </div>
                        <p class="copyright-text">
                            &copy; <%= new Date().getFullYear() %> Korea-DB Education System. 
                            Được thiết kế dành riêng cho giáo viên.
                        </p>
                    </div>
                    
                    <div class="footer-links">
                        <a href="/teacher/privacy" class="footer-link">Chính sách bảo mật</a>
                        <a href="/teacher/terms" class="footer-link">Điều khoản sử dụng</a>
                        <a href="/teacher/guidelines" class="footer-link">Hướng dẫn giảng dạy</a>
                        <a href="/teacher/code-of-conduct" class="footer-link">Quy tắc ứng xử</a>
                    </div>
                    
                    <div class="back-to-top" id="backToTop">
                        <i class="fas fa-chevron-up"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</footer>

<style>
    .teacher-footer {
        background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
        position: relative;
        overflow: hidden;
        margin-top: auto;
    }
    
    .wave-container {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        overflow: hidden;
        line-height: 0;
        transform: rotate(180deg);
    }
    
    .wave {
        position: relative;
        display: block;
        width: calc(100% + 1.3px);
        height: 60px;
        animation: teacherWave 12s ease-in-out infinite;
    }
    
    .wave-fill {
        fill: #4c51bf;
        opacity: 0.7;
    }
    
    .footer-content {
        position: relative;
        z-index: 2;
        padding-top: 60px;
    }
    
    .footer-section {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.6s ease;
    }
    
    .footer-section.animate {
        opacity: 1;
        transform: translateY(0);
    }
    
    .section-header {
        display: flex;
        align-items: center;
        gap: 16px;
        margin-bottom: 24px;
    }
    
    .section-icon {
        width: 56px;
        height: 56px;
        background: linear-gradient(135deg, #4c51bf, #667eea);
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 22px;
        box-shadow: 0 8px 25px rgba(76, 81, 191, 0.3);
        position: relative;
    }
    
    .section-icon::after {
        content: '';
        position: absolute;
        inset: -2px;
        background: linear-gradient(135deg, #4c51bf, #667eea);
        border-radius: 18px;
        z-index: -1;
        opacity: 0.3;
        filter: blur(8px);
    }
    
    .section-title {
        font-size: 20px;
        font-weight: 700;
        color: white;
        margin: 0;
    }
    
    /* Teaching Resources Section */
    .resource-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 16px;
    }
    
    .resource-link {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 16px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        text-decoration: none;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
        position: relative;
        overflow: hidden;
    }
    
    .resource-link::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(76, 81, 191, 0.2), transparent);
        transition: left 0.5s ease;
    }
    
    .resource-link:hover::before {
        left: 100%;
    }
    
    .resource-link:hover {
        background: rgba(255, 255, 255, 0.08);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(76, 81, 191, 0.2);
        text-decoration: none;
        border-color: rgba(76, 81, 191, 0.3);
    }
    
    .resource-icon {
        width: 48px;
        height: 48px;
        background: linear-gradient(135deg, #10b981, #34d399);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 18px;
        flex-shrink: 0;
        box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
    }
    
    .resource-info {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 4px;
    }
    
    .resource-title {
        color: white;
        font-weight: 600;
        font-size: 15px;
    }
    
    .resource-desc {
        color: #a0aec0;
        font-size: 13px;
    }
    
    /* Class Management Section */
    .management-links {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 8px;
    }
    
    .management-link {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px 16px;
        color: #e2e8f0;
        text-decoration: none;
        border-radius: 8px;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }
    
    .management-link::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        transition: left 0.5s ease;
    }
    
    .management-link:hover::before {
        left: 100%;
    }
    
    .management-link:hover {
        background: rgba(76, 81, 191, 0.1);
        color: white;
        transform: translateX(8px);
        text-decoration: none;
    }
    
    .link-icon {
        width: 32px;
        height: 32px;
        background: linear-gradient(135deg, rgba(76, 81, 191, 0.2), rgba(102, 126, 234, 0.2));
        border-radius: 6px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #4c51bf;
        font-size: 12px;
        flex-shrink: 0;
    }
    
    .link-text {
        flex: 1;
        font-weight: 500;
    }
    
    .link-arrow {
        font-size: 10px;
        opacity: 0;
        transition: all 0.3s ease;
    }
    
    .management-link:hover .link-arrow {
        opacity: 1;
        transform: translateX(4px);
    }
    
    /* Professional Development Section */
    .development-links {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 12px;
    }
    
    .development-link {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 16px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        text-decoration: none;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .development-link:hover {
        background: rgba(255, 255, 255, 0.08);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        text-decoration: none;
        border-color: rgba(76, 81, 191, 0.3);
    }
    
    .dev-icon {
        width: 44px;
        height: 44px;
        background: linear-gradient(135deg, #8b5cf6, #a78bfa);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 16px;
        flex-shrink: 0;
        box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
    }
    
    .dev-info {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 2px;
    }
    
    .dev-title {
        color: white;
        font-weight: 600;
        font-size: 14px;
    }
    
    .dev-desc {
        color: #a0aec0;
        font-size: 12px;
    }
    
    /* Support Section */
    .contact-info {
        margin-bottom: 24px;
    }
    
    .contact-item {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .contact-item:last-child {
        border-bottom: none;
    }
    
    .contact-icon {
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, rgba(76, 81, 191, 0.2), rgba(102, 126, 234, 0.2));
        border: 2px solid rgba(76, 81, 191, 0.3);
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #4c51bf;
        font-size: 12px;
        flex-shrink: 0;
    }
    
    .contact-details {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 2px;
    }
    
    .contact-text {
        color: white;
        font-weight: 500;
        font-size: 14px;
    }
    
    .contact-sub {
        color: #a0aec0;
        font-size: 11px;
    }
    
    .quick-support {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 16px;
        margin-bottom: 24px;
    }
    
    .support-title {
        color: white;
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 12px;
        display: flex;
        align-items: center;
    }
    
    .support-buttons {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }
    
    .support-btn {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 10px 12px;
        border: none;
        border-radius: 8px;
        font-size: 12px;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
    }
    
    .support-btn.primary {
        background: linear-gradient(135deg, #4c51bf, #667eea);
        color: white;
        box-shadow: 0 4px 15px rgba(76, 81, 191, 0.3);
    }
    
    .support-btn.secondary {
        background: rgba(255, 255, 255, 0.1);
        color: #e2e8f0;
        border: 1px solid rgba(255, 255, 255, 0.2);
    }
    
    .support-btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 6px 20px rgba(76, 81, 191, 0.4);
    }
    
    .teacher-social {
        text-align: center;
    }
    
    .social-title {
        color: white;
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 12px;
    }
    
    .social-links {
        display: flex;
        justify-content: center;
        gap: 12px;
    }
    
    .social-link {
        width: 40px;
        height: 40px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 14px;
    }
    
    .social-link.facebook:hover {
        background: rgba(59, 89, 152, 0.2);
        border-color: #3b5998;
        box-shadow: 0 4px 15px rgba(59, 89, 152, 0.3);
    }
    
    .social-link.linkedin:hover {
        background: rgba(0, 119, 181, 0.2);
        border-color: #0077b5;
        box-shadow: 0 4px 15px rgba(0, 119, 181, 0.3);
    }
    
    .social-link.youtube:hover {
        background: rgba(255, 0, 0, 0.2);
        border-color: #ff0000;
        box-shadow: 0 4px 15px rgba(255, 0, 0, 0.3);
    }
    
    .social-link.discord:hover {
        background: rgba(114, 137, 218, 0.2);
        border-color: #7289da;
        box-shadow: 0 4px 15px rgba(114, 137, 218, 0.3);
    }
    
    /* Teacher Stats Section */
    .teacher-stats-section {
        background: rgba(255, 255, 255, 0.05);
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .stats-header {
        text-align: center;
        margin-bottom: 32px;
    }
    
    .stats-title {
        color: white;
        font-size: 24px;
        font-weight: 700;
        margin-bottom: 8px;
    }
    
    .stats-subtitle {
        color: #a0aec0;
        font-size: 14px;
    }
    
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 32px;
    }
    
    .stat-item {
        text-align: center;
        padding: 24px;
    }
    
    .stat-icon {
        width: 64px;
        height: 64px;
        background: linear-gradient(135deg, rgba(76, 81, 191, 0.2), rgba(102, 126, 234, 0.2));
        border: 2px solid rgba(76, 81, 191, 0.3);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #4c51bf;
        font-size: 24px;
        margin: 0 auto 16px;
        animation: teacherFloat 3s ease-in-out infinite;
    }
    
    .stat-number {
        font-size: 36px;
        font-weight: 700;
        color: white;
        margin-bottom: 8px;
    }
    
    .stat-label {
        color: #a0aec0;
        font-size: 14px;
        font-weight: 500;
    }
    
    /* Footer Bottom */
    .footer-bottom {
        background: rgba(0, 0, 0, 0.3);
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .bottom-content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 20px;
    }
    
    .footer-logo {
        display: flex;
        align-items: center;
        gap: 16px;
        margin-bottom: 8px;
    }
    
    .logo-icon {
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, #4c51bf, #667eea);
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 16px;
    }
    
    .logo-text {
        display: flex;
        flex-direction: column;
    }
    
    .logo-title {
        color: white;
        font-size: 16px;
        font-weight: 700;
        line-height: 1;
    }
    
    .logo-subtitle {
        color: #a0aec0;
        font-size: 11px;
        font-weight: 500;
    }
    
    .copyright-text {
        color: #a0aec0;
        font-size: 12px;
        margin: 0;
    }
    
    .footer-links {
        display: flex;
        gap: 24px;
        flex-wrap: wrap;
    }
    
    .footer-link {
        color: #a0aec0;
        text-decoration: none;
        font-size: 12px;
        transition: color 0.3s ease;
    }
    
    .footer-link:hover {
        color: #4c51bf;
        text-decoration: none;
    }
    
    .back-to-top {
        width: 44px;
        height: 44px;
        background: linear-gradient(135deg, #4c51bf, #667eea);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(76, 81, 191, 0.3);
    }
    
    .back-to-top:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(76, 81, 191, 0.4);
    }
    
    /* Animations */
    @keyframes teacherWave {
        0%, 100% {
            transform: translateX(0) rotate(180deg);
        }
        50% {
            transform: translateX(-25px) rotate(180deg);
        }
    }
    
    @keyframes teacherFloat {
        0%, 100% {
            transform: translateY(0px);
        }
        50% {
            transform: translateY(-8px);
        }
    }
    
    .slide-up {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.6s ease;
    }
    
    .slide-up.animate {
        opacity: 1;
        transform: translateY(0);
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .wave {
            height: 40px;
        }
        
        .footer-content {
            padding-top: 40px;
        }
        
        .max-w-7xl {
            padding-left: 20px;
            padding-right: 20px;
        }
        
        .grid {
            grid-template-columns: 1fr;
            gap: 32px;
        }
        
        .stats-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }
        
        .stat-number {
            font-size: 28px;
        }
        
        .bottom-content {
            flex-direction: column;
            text-align: center;
            gap: 16px;
        }
        
        .footer-links {
            justify-content: center;
        }
        
        .support-buttons {
            flex-direction: row;
        }
        
        .social-links {
            gap: 8px;
        }
    }
    
    @media (max-width: 480px) {
        .stats-grid {
            grid-template-columns: 1fr;
        }
        
        .section-header {
            flex-direction: column;
            text-align: center;
            gap: 12px;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Initialize teacher footer animations
        initializeTeacherFooterAnimations();
        
        // Initialize teacher stats counters
        initializeTeacherStatsCounters();
        
        // Initialize back to top functionality
        initializeBackToTop();
        
        // Initialize intersection observer for animations
        initializeScrollAnimations();
        
        // Initialize support buttons
        initializeSupportButtons();
        
        // Animation khi tải trang (existing functionality)
        const cards = document.querySelectorAll('.card');
        cards.forEach((card, index) => {
            setTimeout(() => {
                card.style.opacity = '1';
            }, 100 * index);
        });

        // Animation cho biểu đồ tiến độ (existing functionality)
        const progressBars = document.querySelectorAll('.progress-bar-fill');
        setTimeout(() => {
            progressBars.forEach(bar => {
                const height = bar.style.height;
                bar.style.height = '0';
                setTimeout(() => {
                    bar.style.height = height;
                }, 200);
            });
        }, 500);
        
        // Đánh dấu mục menu khi click (existing functionality)
        const navLinks = document.querySelectorAll('.nav-link');
        navLinks.forEach(link => {
            link.addEventListener('click', function() {
                navLinks.forEach(item => item.classList.remove('active'));
                this.classList.add('active');
            });
        });
    });
    
    function initializeTeacherFooterAnimations() {
        // Add staggered animations to footer sections
        const sections = document.querySelectorAll('.footer-section');
        sections.forEach((section, index) => {
            section.style.animationDelay = `${index * 0.1}s`;
        });
        
        // Add hover effects to resource links
        const resourceLinks = document.querySelectorAll('.resource-link, .development-link');
        resourceLinks.forEach(link => {
            link.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-3px) scale(1.02)';
            });
            
            link.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });
    }
    
    function initializeTeacherStatsCounters() {
        const counters = document.querySelectorAll('.stat-number');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const counter = entry.target;
                    const target = parseInt(counter.getAttribute('data-target'));
                    animateTeacherCounter(counter, 0, target, 2500);
                    observer.unobserve(counter);
                }
            });
        });
        
        counters.forEach(counter => observer.observe(counter));
    }
    
    function animateTeacherCounter(element, start, end, duration) {
        const range = end - start;
        const increment = range / (duration / 16);
        let current = start;
        
        const timer = setInterval(() => {
            current += increment;
            if (current >= end) {
                current = end;
                clearInterval(timer);
            }
            
            element.textContent = Math.floor(current).toLocaleString();
        }, 16);
    }
    
    function initializeBackToTop() {
        const backToTop = document.getElementById('backToTop');
        
        backToTop.addEventListener('click', function() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });
        
        // Show/hide based on scroll position
        window.addEventListener('scroll', function() {
            if (window.pageYOffset > 300) {
                backToTop.style.opacity = '1';
                backToTop.style.pointerEvents = 'auto';
            } else {
                backToTop.style.opacity = '0.5';
                backToTop.style.pointerEvents = 'none';
            }
        });
    }
    
    function initializeScrollAnimations() {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animate');
                }
            });
        }, {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        });
        
        document.querySelectorAll('.slide-up').forEach(el => {
            observer.observe(el);
        });
    }
    
    function initializeSupportButtons() {
        // Add ripple effect to support buttons
        const supportButtons = document.querySelectorAll('.support-btn');
        supportButtons.forEach(button => {
            button.addEventListener('click', function(e) {
                createTeacherRippleEffect(e, this);
            });
        });
    }
    
    function openLiveChat() {
        showTeacherNotification('Đang kết nối đến hỗ trợ trực tiếp...', 'info');
        
        // In real implementation, this would open live chat widget
        setTimeout(() => {
            showTeacherNotification('Tính năng chat trực tiếp đang được phát triển', 'warning');
        }, 2000);
    }
    
    function openTicket() {
        // Open ticket creation modal or redirect
        window.open('/teacher/support/create-ticket', '_blank');
    }
    
    function createTeacherRippleEffect(event, element) {
        const ripple = document.createElement('div');
        ripple.style.cssText = `
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.6);
            transform: scale(0);
            animation: teacherRipple 0.6s linear;
            pointer-events: none;
        `;
        
        const rect = element.getBoundingClientRect();
        const size = Math.max(rect.width, rect.height);
        ripple.style.width = ripple.style.height = size + 'px';
        ripple.style.left = (event.clientX - rect.left - size / 2) + 'px';
        ripple.style.top = (event.clientY - rect.top - size / 2) + 'px';
        
        element.style.position = 'relative';
        element.appendChild(ripple);
        
        setTimeout(() => {
            ripple.remove();
        }, 600);
    }
    
    function showTeacherNotification(message, type = 'info') {
        const notification = document.createElement('div');
        notification.className = `teacher-notification teacher-notification-${type}`;
        notification.innerHTML = `
            <div class="teacher-notification-content">
                <i class="fas ${type === 'success' ? 'fa-check-circle' : 
                                type === 'error' ? 'fa-exclamation-triangle' : 
                                type === 'warning' ? 'fa-exclamation-circle' : 'fa-info-circle'}"></i>
                <span>${message}</span>
            </div>
        `;
        
        notification.style.cssText = `
            position: fixed;
            top: 20px;
            right: 20px;
            background: ${type === 'success' ? 'linear-gradient(135deg, #10b981, #34d399)' : 
                          type === 'error' ? 'linear-gradient(135deg, #ef4444, #f87171)' : 
                          type === 'warning' ? 'linear-gradient(135deg, #f59e0b, #fbbf24)' : 
                          'linear-gradient(135deg, #4c51bf, #667eea)'};
            color: white;
            padding: 16px 20px;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            z-index: 2000;
            transform: translateX(100%);
            transition: transform 0.3s ease;
        `;
        
        notification.querySelector('.teacher-notification-content').style.cssText = `
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 14px;
            font-weight: 500;
        `;
        
        document.body.appendChild(notification);
        
        setTimeout(() => {
            notification.style.transform = 'translateX(0)';
        }, 100);
        
        setTimeout(() => {
            notification.style.transform = 'translateX(100%)';
            setTimeout(() => {
                if (notification.parentNode) {
                    notification.parentNode.removeChild(notification);
                }
            }, 300);
        }, 4000);
    }
    
    // Add teacher ripple animation keyframe
    const teacherStyle = document.createElement('style');
    teacherStyle.textContent = `
        @keyframes teacherRipple {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }
    `;
    document.head.appendChild(teacherStyle);
</script>